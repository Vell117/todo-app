<!-- Определяем navbar с сайта materiallize -->
 <nav class="blue darken-4"> 
    <div class="nav-wrapper">
      <a href="/" class="brand-logo">TodosILS</a>
      <ul id="nav-mobile" class="right hide-on-med-and-down">
        
        <!--Для подсветки ссылок todos и create.
        Если isIndex или isCreat, то подсвечиваем ссылку, иначе не подсвечиваем-->
        {{#if isIndex}}
        <!--Ссылка на главную страницу -->
        <li class="active"><a href="/">Todos</a></li>
        {{else}}
        <li><a href="/">Todos</a></li>
        {{/if}}
        
        {{#if isCreate}}
        <!--Ссылка на страницу для создания дел -->
        <li class="active"><a href="/create">Create</a></li>
        {{else}}
        <li><a href="/create">Create</a></li>
        {{/if}}
     
      </ul>
    </div>
  </nav>